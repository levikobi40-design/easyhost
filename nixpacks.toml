# Ensures Node (npm) and Python are available before railway.toml buildCommand runs.
[phases.setup]
nixPkgs = ["python311", "nodejs_22"]

[start]
cmd = "PYTHONPATH=. gunicorn app:app --workers 1 --timeout 120 --bind 0.0.0.0:$PORT"
