[build]
builder = "NIXPACKS"

[deploy]
startCommand = "PYTHONPATH=. /opt/venv/bin/gunicorn -c gunicorn.conf.py app:app"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
