[build]
builder = "NIXPACKS"

[deploy]
startCommand = "PYTHONPATH=. /opt/venv/bin/gunicorn --worker-class eventlet -w 1 --timeout 120 --bind 0.0.0.0:$PORT app:app"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
