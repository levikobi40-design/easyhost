/**
 * Property editor / creator modal — image upload waits for server-confirmed URLs in edit mode.
 * Alias for PropertyCreatorModal (dashboard “PropertyEditor” entry point).
 */
export { default } from './PropertyCreatorModal';
