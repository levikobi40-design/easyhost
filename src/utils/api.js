/**
 * Aliases for dashboard and legacy imports. Canonical implementations live in ../services/api.js.
 */
export { getProperties as fetchProperties, getPropertyTasks as fetchTasks } from '../services/api.js';
